import React from "react";

const Contacts = () => {
  return (
    <div id="call-list">
      <div class="page-title">
        <h1 style={{ fontSize: 20, color: "rgb(42, 196, 32)" }}>
          <strong>Contacts</strong>
        </h1>
        <h2 style={{ margin: 10 }}>You have no contacts!</h2>
      </div>
    </div>
  );
};

export default Contacts;
